# Hadrian Gateway - Environment Variables
# Copy this file to .env and fill in the values
#
# REQUIRED variables have no default and will cause startup to fail if not set
# OPTIONAL variables have sensible defaults or are truly optional

# =============================================================================
# LLM Provider API Keys (at least one required)
# =============================================================================
OPENROUTER_API_KEY=your-openrouter-api-key
ANTHROPIC_API_KEY=your-anthropic-api-key
OPENAI_API_KEY=your-openai-api-key

# =============================================================================
# Database (REQUIRED for PostgreSQL setups)
# =============================================================================
# Generate a strong password: openssl rand -base64 32
POSTGRES_PASSWORD=CHANGE_ME_TO_STRONG_PASSWORD

# For HA setups (postgres-ha, production)
POSTGRES_REPL_PASSWORD=CHANGE_ME_REPLICATION_PASSWORD

# =============================================================================
# Authentication (REQUIRED for OIDC/Keycloak setups)
# =============================================================================
# Keycloak admin password
KEYCLOAK_ADMIN_PASSWORD=CHANGE_ME_ADMIN_PASSWORD

# Keycloak database password
KEYCLOAK_DB_PASSWORD=CHANGE_ME_DB_PASSWORD

# OIDC client secret (generate: openssl rand -base64 32)
OIDC_CLIENT_SECRET=CHANGE_ME_CLIENT_SECRET

# OAuth2 Proxy cookie secret (generate: openssl rand -base64 32 | head -c 32)
OAUTH2_PROXY_COOKIE_SECRET=CHANGE_ME_COOKIE_SECRET

# =============================================================================
# Message Queue (REQUIRED for production)
# =============================================================================
RABBITMQ_PASSWORD=CHANGE_ME_RABBITMQ_PASSWORD

# =============================================================================
# Observability (REQUIRED for production)
# =============================================================================
GRAFANA_PASSWORD=CHANGE_ME_GRAFANA_PASSWORD

# =============================================================================
# Domain & SSL (REQUIRED for production with Traefik)
# =============================================================================
DOMAIN=example.com
ACME_EMAIL=admin@example.com

# =============================================================================
# Optional: Error Tracking
# =============================================================================
# SENTRY_DSN=https://xxx@sentry.io/yyy
